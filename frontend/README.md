# Frontend Scaffold

The frontend is scaffolded with Next.js, React, and Tailwind CSS.

To initialize the frontend, run:

```bash
cd frontend
npx create-next-app@latest . --typescript --tailwind --eslint
```

This will create the Next.js application with all necessary configuration.
